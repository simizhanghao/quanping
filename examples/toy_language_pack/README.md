# Toy LanguagePack (P3-A)

Contract-only fixtures — **no real Belebele/IndoMMLU download**.

| Pack | Language | Notes |
|------|----------|-------|
| `ind_v1` | `ind` | parallel reading + native knowledge + culture stub `NOT_AVAILABLE` |
| `arb_v1` | `arb` (macrolanguage `ara`) | parallel reading only |

```bash
PYTHONPATH=src python -m linguaeval language-inspect-offline \
  configs/examples/21_language_pack_inspect.yaml
```
