# Example-01: Indonesian N2S (Reference Case)

Complex multi-target business workload used to validate the Kernel.

Offline replay reads an existing LlamaFactory N2S prediction JSON
(no re-inference) via adapter `n2s_dialogue_prediction`.

Configure the absolute/relative path in
`configs/examples/n2s_offline_replay.yaml`.
